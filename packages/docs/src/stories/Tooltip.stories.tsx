import type { Meta, StoryObj } from "@storybook/react";
import { Tooltip, TooltipProps } from "@it-ignite-ui/react";

export default {
  title: "Data Display/Tooltip",
  component: Tooltip,
  args: {},
  argTypes: {},
} as Meta<TooltipProps>;

export const Primary: StoryObj<TooltipProps> = {};
